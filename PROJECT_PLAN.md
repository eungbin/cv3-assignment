# CV3 라방바 데이터랩 기술 과제 — 프로젝트 컨텍스트·요구사항·설계서

> 이 문서는 이 프로젝트의 단일 인계 문서다. 이전 대화나 세션 기록을 볼 수 없는
> 개발자 또는 AI 에이전트도 이 파일 하나만 읽고 배경, 요구사항, 결정 이유, 현재
> 구현 상태, 다음 작업, 제출 방법까지 이해할 수 있어야 한다.

## 0. 가장 먼저 알아야 할 내용

- 지원자는 CV3 신입 풀스택 개발자 서류전형에 합격해 기술 과제를 진행 중이다.
- 프로젝트 경로는 `D:\dev\react\cv3-assignment`다.
- 목표는 라방바 데이터랩 과제 페이지의 **라이브 방송/홈쇼핑 테이블을 유형별 최대
  10개까지 정확하게 재현**하는 것이다.
- 확정 스택은 **Vite + React + TypeScript + Express + Cheerio + Tailwind CSS**다.
- 페이지네이션은 구현하지 않는다. “유형별 최대 10개”라는 명시 요구를 우선한다.
- 현재는 프로젝트 기반 설정만 완료됐다. 실제 데이터 수집 API와 방송 테이블은 아직
  구현하지 않았다.
- 다음 작업은 Express에서 과제 페이지를 가져와 Cheerio로 테이블을 파싱하는 것이다.
- 현재 브랜치는 `main`이고 기반 설정 커밋은
  `a4a322b chore: initialize React and Express project`다.
- 이전 검증에서 테스트, 린트, 타입 검사, 빌드, 개발 서버 실행이 모두 성공했고
  `npm audit` 결과는 취약점 0건이었다.

## 1. 사용자 상황과 프로젝트 목적

사용자는 CV3의 신입 풀스택 개발자 채용에 지원했고 서류전형에 합격했다. 회사가
전달한 기술 과제는 디자인 완성도보다 다음을 중요하게 평가한다고 안내한다.

- 실제로 코드를 작성하고 문제를 해결하는 방식
- 화려함보다 실무적인 접근
- 동작하는 결과물
- 명확한 실행 방법
- 작업 단위가 드러나는 Git 커밋
- 제출한 모든 코드를 이해하고 면접에서 설명·수정할 수 있는지

따라서 목표는 많은 기능을 추가하는 것이 아니라, 요구사항을 정확하게 구현하고
데이터 수집·가공·표시 책임을 분리하며 테스트와 README까지 신뢰할 수 있게 만드는
것이다.

## 2. 원본 링크와 정보 우선순위

### 링크

- 채용공고: https://www.wanted.co.kr/wd/374948
- 과제 기본 주소: http://live.ecomm-data.com/assignment
- HTTPS 주소: https://live.ecomm-data.com/assignment
- 라이브 방송: https://live.ecomm-data.com/assignment?type=lb
- 홈쇼핑: https://live.ecomm-data.com/assignment?type=hs

### 충돌 시 우선순위

1. 지원자에게 직접 전달된 기술 과제 안내 문서
2. 과제 예시 페이지에 실제로 표시되는 테이블
3. 이 프로젝트 문서와 구현상의 결정

전달된 문서는 자신이 SSOT(Single Source of Truth)라고 명시한다. 웹페이지와 문서가
충돌하면 전달 문서를 우선한다.

## 3. 채용공고 분석

확인한 공고는 `씨브이쓰리 · 풀스택 개발자 (신입)`이다. 주요 맥락은 다음과 같다.

- CV3는 B2B SaaS 스타트업이다.
- 국내 라이브 커머스·홈쇼핑·공구 데이터를 수집·분석하는 라방바 데이터랩을
  운영한다.
- 광고 통합 운영 솔루션 애즈순 등 여러 제품을 운영한다.
- 신입도 실제 운영 서비스를 프론트엔드부터 백엔드까지 경험한다.

공고상 주요 업무와 역량:

- 라방바 데이터랩 프론트엔드·백엔드 유지보수와 안정적 운영
- 버그 수정, 개선, 장애 대응
- React + TypeScript 기반 화면 개발
- Node.js/Express 계열 API 서버
- 크롤링·배치 파이프라인
- MySQL 쿼리와 데이터 정합성 관리
- 기존 코드 읽기와 디버깅
- JavaScript/TypeScript, CS 기초, SQL, Linux/CLI
- 질문과 피드백에 열린 태도, 학습 의지, 담당 영역에 대한 오너십

이 과제는 작은 화면 구현처럼 보이지만, 실제로는 다음을 확인하기 좋은 형태다.

- React + TypeScript 기본기
- Express에서 외부 데이터 확보
- HTML 파싱 및 서로 다른 유형의 데이터 정규화
- 값 정확성과 오류 처리
- 과도하게 만들지 않는 판단
- 테스트, 문서, Git 사용 습관

## 4. 회사가 전달한 기술 과제 안내

### 기한

- 전달받은 시점에서 `d + 7, 19:00`까지다.
- 전달받은 정확한 날짜가 대화에 기록되지 않았으므로 실제 마감 일시는 아직 계산할
  수 없다.
- 구현을 이어가기 전에 사용자가 전달받은 날짜와 시간대를 확인해야 한다. 회사와
  지원자 모두 한국이므로 별도 안내가 없다면 KST로 보는 것이 자연스럽지만 추측으로
  마감일을 문서에 확정하지 않는다.

### 제출 방법

- 이메일 제목: `[기술과제제출] {자유롭게 작성}_[지원자 이름]`
- 제출 이메일: `known@cv-3.com`
- 제목 앞부분에 `[기술과제제출]`을 반드시 넣는다.
- 제목에 지원자 성함을 반드시 넣는다.
- GitHub 저장소 링크로 제출한다.

### 기능 요구사항

1. 라방바 데이터랩 방송 리스트를 보여주는 간단한 웹페이지를 만든다.
2. `라이브 방송 / 홈쇼핑` 유형 토글로 목록을 전환한다.
3. 각 유형별로 **최대 10개**의 방송을 테이블로 보여준다.
4. 테이블에는 다음 항목이 들어간다.
   - 방송정보(제목)
   - 분류
   - 방송시간
   - 조회수 또는 시청률
   - 판매량
   - 매출액
   - 상품수
5. 자물쇠 기능은 구현하지 않아도 된다.
6. 다만 각 항목의 값은 과제 테이블과 동일해야 한다.
7. 헤더, 푸터, 사이드바는 구현하지 않아도 된다. 과제 링크의 테이블만 완성한다.
8. UI는 최대한 간결하게 만든다. 디자인 완성도는 평가 대상이 아니다.
9. 웹페이지와 안내 문서가 충돌하면 안내 문서를 우선하며, 안내 문서가 SSOT다.

### 데이터 요구사항

- 별도 API는 제공되지 않는다.
- 데이터는 과제 링크에서 직접 확보한다.
- 데이터를 가져오는 방법은 지원자가 판단한다.

### 저장소와 README 요구사항

- GitHub 저장소로 제출한다.
- `README.md`에 실행 방법을 반드시 적는다.
- 평가자가 README만 보고 그대로 실행할 수 있어야 한다.
- 작업 과정을 볼 수 있도록 커밋을 하나로 합치지 않고 작업 단위로 나눈다.

### AI 도구 안내

- 전달 문서에는 챗봇과 코딩 에이전트를 포함한 AI 도구 사용을 제한하지 않는다고
  명시돼 있다.
- 제출한 코드는 모두 지원자가 이해해야 한다.
- 면접에서 직접 설명하고 일부를 수정할 수 있다.
- 본인이 이해하지 못하는 코드는 통과하기 어렵다고 안내한다.

### 예상 소요 시간

- 회사는 부담 없이 이틀 안에 마무리할 수 있는 범위로 설계했다고 안내한다.
- 완벽한 다듬기보다 동작하는 결과물과 명확한 실행 방법에 집중한다.

## 5. 원본 사이트 접근 및 데이터 조사 결과

조사 시점은 2026-07-22 KST다. 데이터는 시간이 지나면 변경될 수 있으므로 아래의
개별 방송명이나 숫자를 하드코딩하면 안 된다.

### 접근 결과

- 채용공고는 정상 접근됐다.
- 과제의 HTTP 주소는 HTTPS로 리디렉션된다.
- `https://live.ecomm-data.com/assignment` 직접 응답은 HTTP 200이었다.
- 일반 웹 리더는 robots/safe URL 정책 때문에 과제 상세 경로를 읽지 못했지만,
  서버에서 직접 HTTP 요청하면 정상 HTML을 받을 수 있었다. 이는 사이트 장애가
  아니다.

### 유형 전환

- 기본 또는 `type=lb`: 라이브 방송
- `type=hs`: 홈쇼핑
- 두 주소 모두 서버 렌더링된 완성 테이블 HTML을 응답한다.

### 확인한 테이블 구조

라이브 방송 헤더:

```text
순위 | 방송정보 | 분류 | 방송시간 | 조회수 | 판매량 | 매출액 | 상품수
```

홈쇼핑 헤더:

```text
순위 | 방송정보 | 분류 | 방송시간 | 시청률 | 판매량 | 매출액 | 상품수
```

두 유형의 차이는 다섯 번째 지표가 라이브는 `조회수`, 홈쇼핑은 `시청률`이라는
점이다.

### 데이터 수와 표시값

- 페이지의 내부 Next.js 데이터(`__NEXT_DATA__`)에는 유형별 15개 항목이 들어올 수
  있다.
- 실제 데스크톱 테이블은 처음 10개만 렌더링한다.
- 과제 요구도 유형별 최대 10개이므로 실제 테이블의 처음 10개만 사용한다.
- 원본 순서를 유지하며 자체 정렬을 추가하지 않는다.
- 공개 상태에서 조회수/시청률, 판매량, 매출액의 내부 값은 `null`이고 테이블에는
  `🔒 로그인`으로 표시된다.
- 인증을 우회하거나 실제 값을 추정하지 않는다.
- 잠금 해제 동작은 만들지 않지만, 값 정확성을 위해 현재 공개 테이블의
  `🔒 로그인` 표시 문자열은 그대로 유지하는 것이 가장 안전하다.
- 일부 라이브 방송의 분류는 내부적으로 카테고리 ID만 있고 실제 테이블의 분류
  칸은 비어 있었다. 빈 값을 `-`나 추정 카테고리로 바꾸지 않는다.
- 홈쇼핑 데이터에는 `cat.cat_name` 형태로 분류명이 제공되는 경우가 있었고 실제
  테이블에도 예를 들어 `식품`처럼 표시됐다.
- 라이브와 홈쇼핑의 내부 JSON 필드 구조는 서로 다르다.

조사 당시 첫 행 예시는 다음과 같았지만, 동적 데이터이므로 테스트 기대값이나
소스 코드에 고정하지 않는다.

- 라이브: 방송시간 `26.07.22 (수) 16:00`, 상품수 `57`, 잠금 지표는 `🔒 로그인`
- 홈쇼핑: `임성근한우국밥 / NS홈쇼핑`, 분류 `식품`, 방송시간
  `26.07.22 (수) 17:10`, 상품수 `2`

### AI 규정 충돌

과제 페이지 HTML의 숨겨진 메타데이터와 구조화 데이터에는 AI 사용 금지 문구가
있었다. 그러나 지원자에게 직접 전달된 문서는 다음 두 가지를 명시한다.

1. AI 도구 사용을 허용한다.
2. 웹페이지와 충돌하면 전달 문서가 우선하는 SSOT다.

따라서 현재 프로젝트는 전달 문서 기준으로 AI 사용이 허용된다고 판단한다. 다만
제출 리스크를 없애려면 담당자에게 아래처럼 서면 확인하는 것을 권장한다.

> 전달받은 과제 문서에는 AI 도구 사용이 허용되며 문서를 SSOT로 안내하고 있으나,
> 과제 페이지 소스에는 AI 사용 금지 문구가 확인됩니다. 전달 문서 기준으로 AI
> 도구를 사용해도 되는지 확인 부탁드립니다.

답변이 오기 전에도 전달 문서 기준으로 개발할 수 있지만, 받은 답변은 이 문서에
추가해야 한다.

## 6. 대화에서 확정한 기술·범위 결정

### Next.js 대신 React + Express

초기에는 하나의 프로세스로 React 화면과 Node API를 구성하기 쉬운 Next.js도
검토했다. 사용자는 채용공고에 React와 Express가 언급돼 있으므로 해당 조합이 더
적합하다고 판단했고, 최종적으로 다음 구성을 확정했다.

- Vite + React + TypeScript 프론트엔드
- TypeScript Express API 서버
- 개발 중 Vite 프록시로 `/api` 연결
- 프로덕션에서는 Express가 Vite `dist` 정적 파일도 제공

이 선택은 공고 스택을 직접 보여주고, 프론트엔드와 백엔드의 책임을 면접에서
명확하게 설명하기 위한 것이다.

### Tailwind CSS 사용

일반 CSS만으로도 충분한 작은 UI지만 사용자가 평소 Tailwind CSS를 주로 사용한다.
익숙한 도구는 제한된 시간과 현장 수정에서 장점이 있으므로 Tailwind 사용을
확정했다.

단, 다음은 하지 않는다.

- 별도 UI 컴포넌트 라이브러리 추가
- 과제에 필요하지 않은 디자인 시스템 구축
- 과도한 애니메이션이나 장식
- 단 한 번만 쓰는 스타일을 위한 불필요한 추상화

### Cheerio의 역할

Cheerio는 네트워크 요청 도구가 아니라 **받아온 HTML 문자열을 탐색·파싱하는
Node.js 라이브러리**다.

```text
Express fetch -> HTML 응답 -> Cheerio 파싱 -> 표시용 JSON -> React
```

넓은 의미에서는 크롤링이라고 말할 수 있지만, 더 정확한 표현은 정해진 두 URL에서
공개 데이터를 추출하는 **서버 사이드 웹 스크래핑**이다.

- `fetch`: 과제 페이지 HTML을 가져온다.
- `Cheerio`: HTML의 테이블 행과 셀을 선택해 텍스트를 추출한다.
- 전체 흐름: 웹 스크래핑
- 다수의 페이지를 발견·순회하며 수집하는 시스템: 일반적인 의미의 크롤러

면접에서는 “Express에서 HTML을 가져오고 Cheerio로 필요한 테이블을
파싱했습니다”라고 설명한다.

### 페이지네이션을 구현하지 않음

사용자는 원본 내부 데이터가 10개를 넘을 경우 다음 페이지에서 보여주는 방안을
제안했다. 검토 결과 다음 이유로 구현하지 않기로 했고 사용자가 동의했다.

- 문서는 “페이지당 10개”가 아니라 “각 유형별 최대 10개”라고 명시한다.
- 페이지네이션으로 11번째 항목을 보여주면 명시된 최대 범위를 넘는다.
- 원본 예시 테이블도 페이지네이션 없이 10개만 렌더링한다.
- 11번째 이후 데이터는 실제 예시 테이블에 표시되지 않아 값 동일성 비교 대상이
  불명확하다.
- 내부 Next.js 데이터를 별도로 해석해야 하므로 구현 복잡도와 결합도가 증가한다.
- 과제 범위 밖 기능은 실무적 단순성이라는 평가 방향과 맞지 않는다.

최종 결정은 **각 유형의 원본 테이블 순서대로 최대 10개만 표시**하는 것이다.

### 렌더링된 HTML 테이블을 데이터 계약으로 사용

내부 `__NEXT_DATA__`를 직접 정규화하는 방법도 검토했지만 다음 이유로 실제로
렌더링된 데스크톱 테이블 HTML을 파싱하는 방향을 선택했다.

- 과제는 내부 데이터가 아니라 테이블 표시값과 같아야 한다.
- 라이브와 홈쇼핑의 내부 필드 구조가 다르다.
- 내부 `null`이 화면에서는 `🔒 로그인`으로 포맷된다.
- 일부 라이브 분류는 내부 ID가 있어도 실제 화면에는 비어 있다.
- 날짜, 요일, 숫자, 잠금 문자열의 표시 포맷을 그대로 보존할 수 있다.

브라우저 자동화 도구는 사용하지 않는다. 현재 HTML 응답에 완성된 테이블이 있어
Cheerio만으로 충분하며, Playwright/Puppeteer는 느리고 복잡하며 과제 범위에 비해
과하다.

## 7. 확정 기술 스택과 현재 버전

`package.json`과 `package-lock.json`이 실제 버전의 최종 기준이다. 프로젝트 기반
설정 당시 주요 버전은 다음과 같다.

### 실행 의존성

- React `19.2.x`
- React DOM `19.2.x`
- Express `5.2.x`
- Cheerio `1.2.x`

### 개발 의존성

- Vite `8.1.x`
- TypeScript `6.0.x`
- Tailwind CSS `4.3.x`
- `@tailwindcss/vite` `4.3.x`
- Vitest `4.1.x`
- Testing Library
- ESLint `10.x`
- `tsx`
- `concurrently` `9.2.4`

`concurrently` 최신 10.x 설치 시 하위 `shell-quote`에 대한 high severity 개발
의존성 감사 결과가 나와, 취약점이 해소되는 `9.2.4`로 고정했다. 이후 전체
`npm audit`은 취약점 0건이었다.

### 런타임

- 개발에 사용한 Node.js: `22.17.0`
- `package.json` 요구 버전: Node.js `>=22.12.0`
- `.nvmrc`: `22.17.0`
- 개발 당시 npm: `11.6.0`

## 8. 현재 저장소 상태

### 위치와 Git

```text
D:\dev\react\cv3-assignment
branch: main
setup commit: a4a322b chore: initialize React and Express project
```

원격 GitHub 저장소는 아직 연결하거나 푸시하지 않았다. 새 세션에서는 먼저 다음으로
실제 상태를 확인한다.

```bash
git status --short --branch
git log --oneline -5
```

Windows 샌드박스에서 Git이 dubious ownership을 경고하면 전역 설정을 바꾸지 말고
명령별로 다음 옵션을 사용할 수 있다.

```bash
git -c safe.directory=D:/dev/react/cv3-assignment status
```

### 현재 구현된 파일과 역할

```text
server/index.ts          Express 서버, /api/health, 프로덕션 정적 파일 제공
src/App.tsx              기반 설정 완료를 보여주는 임시 화면
src/App.test.tsx         임시 화면 렌더링 스모크 테스트
src/main.tsx             React 진입점
src/index.css            Tailwind import와 최소 전역 스타일
src/test-setup.ts        Testing Library matcher 설정
vite.config.ts           React/Tailwind 플러그인과 /api 프록시
vitest.config.ts         jsdom 테스트 환경
eslint.config.js         TypeScript/React ESLint 설정
tsconfig.*.json          브라우저, 도구, Express 빌드 설정 분리
README.md                현재 설치·실행 방법
PROJECT_PLAN.md          현재 읽고 있는 단일 인계 문서
```

### 현재 구현된 서버 동작

```http
GET /api/health
200 { "status": "ok" }
```

개발 포트:

- Vite React: `http://localhost:5173`
- Express API: `http://localhost:3001`
- Vite가 `/api` 요청을 Express로 프록시한다.

빌드 후 `dist`가 존재하면 Express가 정적 React 앱을 제공하므로 다음 순서로 실행할
수 있다.

```bash
npm run build
npm start
# http://localhost:3001
```

### 프로젝트 생성 과정 참고

처음 Vite 생성 명령이 npm 11에서 `--template react-ts`를 잘못 해석해 바닐라
TypeScript 템플릿을 만들었다. 해당 생성물은 React 템플릿 구조로 바로잡았고 기본
데모 파일과 이미지는 제거했다. 현재 코드에는 바닐라 템플릿 의존성이 남아 있지
않으며, 이 이력 때문에 다시 프로젝트를 생성할 필요는 없다.

### 마지막 검증 결과

다음 명령이 모두 성공했다.

```bash
npm test
npm run lint
npm run typecheck
npm run build
npm audit
```

추가로 `npm run dev` 실행 후 다음을 확인했다.

- `http://localhost:5173`: HTTP 200
- `http://localhost:3001/api/health`: HTTP 200 및 `{"status":"ok"}`

## 9. 목표 아키텍처

```text
사용자 브라우저
  |
  | GET /api/broadcasts?type=lb 또는 type=hs
  v
React + TypeScript
  |
  | same-origin /api 요청 (개발 중 Vite proxy)
  v
Express API
  |
  | 고정된 과제 URL을 서버 fetch
  v
라방바 과제 페이지의 SSR HTML
  |
  | Cheerio로 방송 테이블 파싱
  v
표시용 BroadcastTable JSON
  |
  v
React 토글 + 최대 10행 테이블
```

이 구조의 이유:

- 브라우저가 원본 사이트를 직접 요청할 때 생길 수 있는 CORS 문제를 피한다.
- 원본 HTML 구조 의존성을 Express 내부에 한정한다.
- React는 데이터 표시와 사용자 상호작용에만 집중한다.
- 채용공고의 React, Express, 크롤링/배치 경험과 직접 연결된다.
- 한 저장소와 `npm run dev` 하나로 평가자가 실행할 수 있다.

데이터베이스는 사용하지 않는다. 저장해야 할 사용자 데이터나 영속 상태가 없으며
MySQL을 추가하는 것은 과제 범위를 벗어난다.

## 10. 목표 API 계약

### 엔드포인트

```http
GET /api/broadcasts?type=lb
GET /api/broadcasts?type=hs
```

### 유형

```ts
type BroadcastType = 'lb' | 'hs'
```

- `lb`: 라이브 방송
- `hs`: 홈쇼핑
- 누락 시 `lb`를 기본값으로 사용하고, 잘못된 명시값은 400으로 처리한다.

### 표시용 모델

```ts
interface BroadcastRow {
  rank: string
  title: string
  platform: string
  category: string
  broadcastTime: string[]
  audience: string
  sales: string
  revenue: string
  productCount: string
}

interface BroadcastTable {
  type: BroadcastType
  audienceLabel: '조회수' | '시청률'
  rows: BroadcastRow[]
}
```

표시 정확성이 목적이므로 숫자나 `Date`로 변환했다가 다시 포맷하지 않는다. 원본
테이블의 표시 문자열을 필요한 공백만 정리해 전달한다.

### 상태 코드

- 성공: `200`
- 잘못된 유형: `400`
- 원본 페이지 요청 실패, 비정상 상태, 파싱 계약 불일치: `502`
- 예상하지 못한 내부 오류: `500`

클라이언트에 원본 HTML, 내부 스택, 민감한 디버그 정보는 반환하지 않는다.

## 11. Express 데이터 수집 설계

### 권장 파일 분리

```text
server/
  index.ts                 서버 생성과 실행
  routes/broadcasts.ts     쿼리 검증과 HTTP 응답
  services/assignment.ts   고정 URL fetch와 타임아웃
  parsers/broadcasts.ts    Cheerio 테이블 파싱
  types.ts                 서버 응답 타입
```

파일 수가 과도해진다면 `routes`와 `service`를 합쳐도 된다. 중요한 경계는 네트워크
요청과 순수 HTML 파서를 분리해 파서를 외부 네트워크 없이 테스트할 수 있게 하는
것이다.

### 원본 URL 매핑

```ts
const ASSIGNMENT_URLS = {
  lb: 'https://live.ecomm-data.com/assignment?type=lb',
  hs: 'https://live.ecomm-data.com/assignment?type=hs',
} as const
```

사용자 입력을 URL에 그대로 붙이지 않는다. 허용된 두 값을 고정 URL로 매핑해
SSRF와 임의 프록시 사용 가능성을 막는다.

### fetch 기준

- Node 내장 `fetch`를 사용한다.
- `AbortSignal.timeout(...)` 또는 `AbortController`로 명시적 타임아웃을 둔다.
- 리디렉션 후 HTTP 상태가 성공인지 검사한다.
- HTML 본문이 비어 있으면 실패로 본다.
- 자격증명, 쿠키, 로그인 세션을 사용하지 않는다.
- 일반 공개 페이지에 유형당 한 번만 요청하며 공격적인 반복 요청을 하지 않는다.

지속적인 서버 캐시는 일단 사용하지 않는다. React가 현재 세션에서 유형별 응답을
보관해 같은 토글을 반복할 때 불필요한 재요청을 줄인다. 원본 서버 부하가 실제로
문제가 될 때만 짧은 TTL 캐시를 검토한다.

## 12. Cheerio 파서 설계

### 핵심 원칙

- CSS Module의 해시 클래스명에 의존하지 않는다.
- 페이지의 여러 `table` 중 헤더에 `방송정보`가 있는 테이블을 찾는다.
- 필수 헤더 순서를 검증한다.
- `tbody > tr`을 원본 순서대로 읽는다.
- 최대 10개만 반환한다.
- 방송정보 셀에서는 제목과 플랫폼을 구조적으로 분리한다.
- 방송시간의 두 줄은 `string[]`으로 유지한다.
- 불필요한 연속 공백만 정리하고 이모지와 의미 있는 문자는 유지한다.
- 빈 분류는 빈 문자열로 유지한다.
- `🔒 로그인`은 인증 기능 없이 표시 문자열로만 유지한다.
- 테이블 또는 필수 셀이 없으면 빈 데이터로 가장하지 않고 파싱 오류를 발생시킨다.

대략적인 흐름:

```ts
const $ = cheerio.load(html)

const table = $('table').filter((_, element) => {
  const headers = $(element).find('thead th').map((_, th) => $(th).text().trim()).get()
  return headers.includes('방송정보')
}).first()

const rows = table.find('tbody > tr').slice(0, 10)
```

위 코드는 개념 예시다. 실제 구현에서는 헤더 검증, 제목/플랫폼 분리, 시간 배열,
오류 타입과 테스트를 포함해야 한다.

### HTML 대신 내부 JSON을 쓰지 않는 이유

내부 JSON은 데이터 수집에는 편할 수 있지만 이 과제의 계약은 “표시값 동일”이다.
렌더링된 HTML을 읽으면 다음을 별도로 복제할 필요가 없다.

- 라이브/홈쇼핑별 필드 이름 차이
- 날짜와 요일 포맷
- `null`에서 `🔒 로그인`으로의 변환
- 빈 카테고리 표시
- 상품수와 텍스트 포맷

원본 사이트가 향후 CSR로 바뀌어 HTML에 테이블이 없어질 경우에만 내부 JSON 또는
다른 수집 방식을 재검토한다. 현재 시점에는 fallback을 미리 만들지 않는다.

## 13. React UI 설계

### 화면 범위

화면에는 다음만 둔다.

- 페이지 제목 또는 짧은 설명(필요 최소한)
- `라이브 방송`, `홈쇼핑` 유형 토글
- 방송 테이블
- 로딩 상태
- 오류 메시지와 재시도 버튼

헤더, 푸터, 사이드바, 검색, 정렬, 필터, 상세 페이지, 로그인은 만들지 않는다.

### 토글 동작

- 초기 선택은 라이브 방송(`lb`)이다.
- 두 버튼을 `tablist`/`tab` 또는 동등하게 접근 가능한 구조로 구현한다.
- 선택 상태를 색상뿐 아니라 `aria-selected` 등으로 전달한다.
- 선택한 유형의 데이터가 없을 때 `/api/broadcasts`를 호출한다.
- 한 번 성공한 유형은 현재 페이지 세션 동안 유형별로 보관한다.
- 빠르게 토글해도 늦게 끝난 이전 요청이 현재 유형 화면을 덮지 않아야 한다.
- 단순한 구현은 `lb`와 `hs` 응답 상태를 별도 key로 저장하는 것이다.

### 테이블

- 원본 순위와 순서를 그대로 사용한다.
- 행 수는 최대 10개다.
- 라이브는 `조회수`, 홈쇼핑은 `시청률` 헤더를 표시한다.
- 긴 제목이 전체 레이아웃을 깨지 않도록 충분한 너비와 줄바꿈/말줄임을 정한다.
- 좁은 화면에서는 카드 UI를 새로 만들지 않고 테이블 컨테이너에 가로 스크롤을 둔다.
- `th scope="col"` 등 기본 테이블 접근성을 지킨다.
- 원본 사이트 전체 디자인을 복제하지 않는다.

### 스타일

- Tailwind CSS를 사용한다.
- 간결한 중립색 테이블과 선택된 토글을 알아볼 수 있는 강조색이면 충분하다.
- 디자인 완성도가 평가 대상이 아니므로 기능과 가독성을 우선한다.
- 상용 UI 라이브러리를 추가하지 않는다.

## 14. 오류·보안·운영 기준

- Express는 `x-powered-by`를 비활성화한 상태다.
- API의 `type`은 allowlist로 검증한다.
- 원본 URL은 고정 매핑한다.
- 원본 요청에는 타임아웃을 둔다.
- React에 HTML을 직접 삽입하지 않고 추출된 텍스트 JSON만 전달한다.
- React 기본 이스케이프를 유지하며 `dangerouslySetInnerHTML`을 사용하지 않는다.
- 원본 요청 실패 시 오래된 fixture를 실제 데이터처럼 자동 표시하지 않는다.
- 오류 화면에는 재시도를 제공한다.
- 로컬 테스트 fixture는 실제 동적 방송 데이터를 대량 복사하지 않고 필요한 구조만
  가진 합성 HTML로 작성한다.
- 로깅은 개발에 필요한 최소 수준으로 유지하고 원본 HTML 전체를 로그로 남기지 않는다.

## 15. 테스트 계획과 완료 기준

### 파서 단위 테스트

외부 네트워크에 의존하지 않는 합성 HTML fixture를 사용한다.

- 라이브 테이블 필드 추출
- 홈쇼핑 테이블 필드 추출
- `조회수`와 `시청률` 헤더 구분
- 제목과 플랫폼 분리
- 방송시간 두 줄 추출
- 빈 분류 보존
- `🔒 로그인` 보존
- 원본 순서 보존
- 10개 초과 행을 10개로 제한
- 관련 없는 다른 테이블 무시
- 필수 테이블 또는 헤더 누락 시 오류

### API 테스트

- `lb` 성공
- `hs` 성공
- 잘못된 `type`에 400
- 원본 fetch 실패에 502
- 파서 실패에 502
- 외부 네트워크를 mock해 테스트가 안정적으로 실행됨

### UI 테스트

- 기본 라이브 선택
- 초기 로딩 상태
- 라이브 데이터 렌더링
- 홈쇼핑 토글 시 요청과 `시청률` 헤더 표시
- 성공한 유형 응답 재사용
- 오류 메시지와 재시도
- 빠른 토글 시 현재 유형 데이터 유지

### 전체 완료 기준

```bash
npm test
npm run lint
npm run typecheck
npm run build
```

모든 명령이 성공해야 한다. 이후 실제 과제 페이지의 `lb`, `hs` 화면과 각각 다음을
수동 비교한다.

- 헤더
- 행 수
- 첫 번째와 열 번째 행
- 제목과 플랫폼
- 분류의 빈 값 포함
- 방송시간
- 잠금 표시
- 상품수

## 16. 작업 순서와 Git 커밋 계획

첫 커밋은 이미 완료됐다.

```text
a4a322b chore: initialize React and Express project
```

권장 후속 작업:

1. Express fetch 서비스와 순수 Cheerio 파서를 테스트 우선으로 구현한다.
2. `/api/broadcasts` 라우트를 구현하고 오류 상태를 검증한다.
3. React 토글, 요청 상태, 테이블을 구현한다.
4. UI 상호작용 테스트와 실제 데이터 수동 비교를 수행한다.
5. README에 최종 구조, 데이터 취득 방식, 제한사항을 반영한다.

권장 커밋:

```text
feat: add assignment table fetcher and parser
feat: expose broadcast list API
feat: implement broadcast type tabs and table
test: cover broadcast loading and error states
docs: document final setup and design decisions
```

실제 작업 단위가 합칠 수 있을 정도로 작다면 API와 파서 커밋을 하나로 유지해도
된다. 반대로 의미 없는 미세 커밋을 만들지 않는다. 제출 전에 커밋을 하나로
squash하지 않는다.

## 17. README 최종 요구사항

현재 README에는 기반 실행 방법이 있다. 기능 완성 후 다음을 모두 확인한다.

- 프로젝트 목적
- 기술 스택
- 검증된 Node/npm 버전
- `npm ci`
- `npm run dev`
- 웹/API 주소
- 테스트, 린트, 타입 검사, 빌드 명령
- 프로덕션 빌드와 실행 방법
- 데이터가 과제 페이지에서 서버 사이드 스크래핑된다는 설명
- Cheerio의 역할
- 유형별 최대 10개와 페이지네이션 미구현 이유
- 잠금 항목은 공개 화면 표시값을 보존한다는 설명
- 원본 페이지 실패 시 동작
- 구현하지 않은 범위
- 주요 설계 결정과 트레이드오프

평가자는 README만 보고 실행하므로 불필요한 사전 작업이나 암묵적 환경변수가 없어야
한다. 필요 환경변수가 생기면 `.env.example`과 설명을 함께 추가한다.

## 18. 최종 제출 체크리스트

개발 완료 후 다음 순서로 확인한다.

- [ ] 전달 날짜를 확인해 정확한 마감 `d + 7, 19:00`을 기록한다.
- [ ] AI 사용 규정 충돌을 담당자에게 확인했으면 답변을 기록한다.
- [ ] 라이브/홈쇼핑 각각 최대 10개가 실제 테이블과 일치한다.
- [ ] 잠금 해제를 구현하거나 인증을 우회하지 않았다.
- [ ] `npm ci`를 깨끗한 환경에서 실행해 본다.
- [ ] 테스트, 린트, 타입 검사, 빌드가 모두 성공한다.
- [ ] README 명령을 처음 보는 사람의 관점에서 따라 해 본다.
- [ ] Git 커밋이 작업 단위로 나뉘어 있다.
- [ ] `.env`, 로그, 개인 정보, 불필요한 원본 HTML이 커밋되지 않았다.
- [ ] GitHub 저장소를 평가자가 접근할 수 있다.
- [ ] 제출 이메일에 GitHub URL을 포함한다.
- [ ] 제목이 `[기술과제제출] {자유롭게 작성}_[지원자 이름]` 형식이다.
- [ ] 수신자가 `known@cv-3.com`인지 확인한다.

## 19. 면접에서 설명할 핵심

지원자는 모든 코드를 직접 설명하고 수정할 수 있어야 한다. 최소한 다음 질문에
답할 수 있어야 한다.

- 브라우저에서 원본 사이트를 직접 요청하지 않고 Express를 둔 이유
- Cheerio가 하는 일과 `fetch`가 하는 일의 차이
- 크롤링과 웹 스크래핑의 차이
- 내부 `__NEXT_DATA__` 대신 렌더링된 테이블을 파싱한 이유
- 라이브와 홈쇼핑의 지표 헤더가 다른 점
- `null` 지표를 추정하지 않고 `🔒 로그인`으로 보존한 이유
- 페이지네이션을 추가하지 않은 이유
- URL allowlist가 SSRF를 막는 방식
- 네트워크 없이 파서를 테스트하는 방법
- 원본 HTML 구조가 바뀌었을 때 오류가 어떻게 드러나는지
- 빠른 토글의 응답 순서 문제를 어떻게 피했는지
- 왜 데이터베이스와 UI 라이브러리를 추가하지 않았는지

## 20. 다음 세션의 즉시 실행 절차

이전 대화 없이 이 문서로 작업을 재개한다면 다음 순서를 따른다.

1. `D:\dev\react\cv3-assignment`에서 Git 상태와 최근 커밋을 확인한다.
2. `npm ci`가 필요한지 `node_modules` 상태를 확인한다.
3. `npm test`, `npm run lint`, `npm run typecheck`, `npm run build`로 기준 상태를
   확인한다.
4. 이 문서의 11~12절에 따라 외부 fetch 서비스와 순수 Cheerio 파서를 구현한다.
5. 합성 HTML fixture로 파서 테스트를 먼저 통과시킨다.
6. `/api/broadcasts?type=lb|hs`를 연결한다.
7. 실제 두 원본 URL로 수동 확인한다.
8. 기능 단위로 커밋한다.

현재 즉시 수행할 구체적인 다음 목표:

> 라이브와 홈쇼핑 합성 HTML fixture로 실패하는 파서 테스트를 작성한 뒤,
> 원본 테이블의 표시 문자열을 최대 10행까지 추출하는 Cheerio 파서를 구현한다.

## 21. 아직 해결되지 않은 사항

- 과제를 실제로 전달받은 날짜가 없어 제출 마감 시각을 계산하지 못했다.
- AI 사용 규정 충돌에 대한 회사 담당자의 확인 여부가 기록되지 않았다.
- GitHub 원격 저장소 이름과 공개/비공개 정책이 정해지지 않았다.
- 실제 기능 구현 전이므로 원본 사이트가 현재도 같은 HTML 구조인지 구현 시점에
  다시 확인해야 한다.

이 네 가지 외의 핵심 범위와 기술 선택은 대화에서 합의됐다. 새로운 증거나 회사의
추가 안내가 없다면 다시 기술 스택, 페이지네이션, 데이터 취득 방식부터 논의하지
말고 위 결정에 따라 구현을 이어간다.
