# CV3 기술 과제 프로젝트 계획서

## 1. 문서 목적과 현재 상태

이 문서는 대화가 중단되거나 개발 담당자가 바뀌더라도 이 파일만 읽고 프로젝트를
계속 진행할 수 있도록 요구사항, 조사 결과, 결정 사항, 구현 계획을 기록한 문서다.

현재 완료된 범위:

- Vite + React + TypeScript 프로젝트 구성
- Express TypeScript 서버와 `/api/health` 구성
- Tailwind CSS 구성
- Vitest, Testing Library, ESLint 구성
- 개발·빌드·실행 스크립트 구성

아직 구현하지 않은 범위:

- 과제 페이지 요청 및 Cheerio 파서
- 방송 목록 API
- 유형 토글과 방송 테이블
- 로딩·오류·재시도 UI
- 파서 및 사용자 동작 테스트

## 2. 참고 링크와 우선순위

- 채용공고: https://www.wanted.co.kr/wd/374948
- 과제 페이지: https://live.ecomm-data.com/assignment
- 라이브 방송: https://live.ecomm-data.com/assignment?type=lb
- 홈쇼핑: https://live.ecomm-data.com/assignment?type=hs

요구사항이 충돌할 때의 우선순위:

1. 지원자에게 전달된 기술 과제 문서
2. 과제 예시 페이지의 실제 표시 내용
3. 이 설계 문서

전달 문서는 자신이 SSOT라고 명시한다. 문서에는 AI 도구 사용을 허용한다고 되어
있지만 과제 페이지 HTML의 숨겨진 메타데이터에는 AI 사용 금지 문구가 있다. 전달
문서가 우선한다는 기준으로 개발하되, 제출 전에 담당자에게 서면 확인하는 것을
권장한다.

## 3. 과제 요구사항

반드시 구현한다:

- 라이브 방송과 홈쇼핑 유형을 토글로 전환한다.
- 유형별 방송을 원본 순서대로 최대 10개만 보여준다.
- 테이블 열은 순위, 방송정보, 분류, 방송시간, 조회수 또는 시청률, 판매량,
  매출액, 상품수로 구성한다.
- 라이브 방송은 `조회수`, 홈쇼핑은 `시청률`을 사용한다.
- 각 셀의 표시값은 과제 예시 테이블과 동일해야 한다.
- README만 보고 설치하고 실행할 수 있어야 한다.
- 커밋은 의미 있는 작업 단위로 나눈다.

구현하지 않는다:

- 11개 이상의 데이터를 보기 위한 페이지네이션
- 헤더, 푸터, 사이드바
- 로그인 및 잠금 해제 기능
- 방송 상세 페이지
- 검색, 정렬, 필터
- 데이터베이스
- 외부 UI 컴포넌트 라이브러리

문서의 “각 유형별 최대 10개”는 페이지당 10개가 아니라 유형 전체에서 최대
10개로 해석한다. 페이지네이션을 추가하면 명시된 범위를 벗어나므로 구현하지 않는다.

## 4. 조사된 원본 페이지 특성

2026-07-22 KST 기준으로 직접 확인한 내용이다.

- HTTP 주소는 HTTPS로 리디렉션되며 최종 응답은 HTTP 200이다.
- `type=lb`와 `type=hs` 쿼리로 유형을 선택한다.
- 서버가 완성된 테이블 HTML을 반환하므로 브라우저 자동화는 필요 없다.
- 페이지 내부 데이터에는 15개가 포함될 수 있지만 실제 데스크톱 테이블은 처음
  10개만 렌더링한다.
- 공개 상태의 조회수/시청률, 판매량, 매출액 원본값은 `null`이며 테이블에는
  `🔒 로그인`으로 표시된다.
- 잠금 동작은 만들지 않지만 표시값의 정확성을 위해 `🔒 로그인` 문자열은 유지한다.
- 일부 라이브 방송의 분류는 실제 테이블에서도 빈 칸이다. 임의로 카테고리를
  추정하거나 `-`를 넣지 않고 빈 값을 유지한다.
- 라이브와 홈쇼핑의 내부 JSON 구조가 다르므로 내부 객체보다 렌더링된 테이블을
  표시 계약으로 삼는 것이 안전하다.

## 5. 기술 선택

### React + Express

채용공고에 제시된 React 및 Express 경험을 직접 보여주고, 프론트엔드와 API의
책임을 명확히 설명하기 위해 선택했다.

- React: 토글, 요청 상태, 테이블 렌더링
- Express: 외부 페이지 요청, HTML 파싱, 오류 변환
- Vite: React 개발 서버와 프로덕션 빌드

개발 중에는 Vite가 `/api` 요청을 `localhost:3001`의 Express로 프록시한다.
프로덕션 빌드 후에는 Express가 `dist` 정적 파일도 제공한다.

### Tailwind CSS

지원자가 익숙한 도구이며 제한된 시간 안에 일관된 UI를 만드는 데 도움이 되므로
사용한다. 별도 UI 라이브러리나 디자인 시스템은 도입하지 않는다.

### Cheerio

Cheerio는 HTML 문자열을 jQuery와 유사한 선택자 문법으로 탐색하는 Node.js
라이브러리다. Cheerio 자체가 네트워크 요청을 하지는 않는다.

1. Express의 `fetch`가 과제 페이지 HTML을 가져온다.
2. Cheerio가 HTML을 로드한다.
3. 테이블의 헤더, 행, 셀 텍스트를 추출한다.
4. React에 필요한 JSON으로 반환한다.

이번 작업은 여러 사이트를 순회하는 범용 크롤러라기보다, 정해진 두 페이지에서
필요한 공개 데이터를 추출하는 서버 사이드 웹 스크래핑에 가깝다.

## 6. 목표 아키텍처

```text
React 브라우저 화면
  -> GET /api/broadcasts?type=lb|hs
Express API
  -> 고정된 라방바 과제 URL을 fetch
  -> Cheerio로 렌더링된 테이블 파싱
  -> 최대 10행을 표시용 모델로 정규화
React 방송 테이블
```

브라우저가 원본 사이트를 직접 요청하지 않기 때문에 CORS 문제를 피하고, 외부
페이지 구조에 의존하는 코드를 서버에 한정할 수 있다.

## 7. API 계약

예정 엔드포인트:

```http
GET /api/broadcasts?type=lb
GET /api/broadcasts?type=hs
```

허용하는 `type`은 `lb`, `hs`뿐이다. 사용자 입력을 원본 URL에 그대로 결합하지
않고 각 유형을 고정 URL에 매핑해 SSRF 가능성을 차단한다.

예정 응답 모델:

```ts
type BroadcastType = 'lb' | 'hs'

interface BroadcastRow {
  rank: string
  title: string
  platform: string
  category: string
  broadcastTime: string[]
  audience: string
  sales: string
  revenue: string
  productCount: string
}

interface BroadcastTable {
  type: BroadcastType
  audienceLabel: '조회수' | '시청률'
  rows: BroadcastRow[]
}
```

정확한 화면 재현이 목적이므로 표시 문자열을 숫자나 `Date`로 변환했다가 다시
포맷하지 않는다. 원본 테이블의 텍스트를 정리해 그대로 전달한다.

## 8. 파서 구현 기준

- 과제 페이지의 테이블 중 `방송정보` 헤더를 가진 테이블을 찾는다.
- `tbody > tr`을 원본 순서대로 읽고 최대 10개만 사용한다.
- 셀 위치와 방송정보 셀 내부의 제목·플랫폼 요소를 구분해 추출한다.
- 불필요한 공백만 정리하고 의미 있는 문자와 이모지는 유지한다.
- 빈 분류는 빈 문자열로 유지한다.
- 테이블 또는 필수 열이 없으면 조용히 빈 배열을 반환하지 말고 파싱 오류를 낸다.
- 원본 요청에는 타임아웃을 적용하고 HTTP 상태를 검사한다.
- API는 잘못된 유형에 400, 원본 요청·파싱 실패에 502를 반환한다.
- 오류 응답은 내부 HTML이나 스택을 노출하지 않는다.

지속적인 서버 캐시는 두지 않는다. React에서는 한 세션 동안 이미 받은 유형을
유형별로 보관해 토글할 때 같은 요청을 반복하지 않는다. 오래된 fixture를 실제
데이터처럼 보여주는 fallback은 값 정확성 요구와 충돌하므로 사용하지 않는다.

## 9. UI 구현 기준

- 초기 선택은 라이브 방송이다.
- 상단에 `라이브 방송`, `홈쇼핑` 두 토글을 둔다.
- 선택 상태는 색상뿐 아니라 `aria-selected` 등 접근성 속성으로 표현한다.
- 데이터를 처음 요청할 때 로딩 상태를 표시한다.
- 유형별 응답을 별도로 저장해 늦게 끝난 이전 요청이 현재 화면을 덮지 않게 한다.
- 오류 시 간결한 메시지와 재시도 버튼을 제공한다.
- 데스크톱은 일반 테이블, 좁은 화면은 가로 스크롤을 제공한다.
- 제목은 충분한 폭을 주고 긴 텍스트는 레이아웃을 깨지 않게 처리한다.
- 디자인은 간결하게 유지하며 예시 사이트의 전체 레이아웃을 복제하지 않는다.

## 10. 테스트와 완료 기준

자동 테스트는 실제 외부 사이트에 의존하지 않는 작은 합성 HTML fixture를 사용한다.

파서 테스트:

- 라이브 테이블 필드 추출
- 홈쇼핑 테이블 필드 추출
- 조회수와 시청률 헤더 구분
- 제목과 플랫폼 분리
- 빈 분류 유지
- `🔒 로그인` 표시 유지
- 10개 초과 행 절단
- 테이블 또는 필수 열 누락 시 오류

UI 테스트:

- 기본 라이브 선택
- 홈쇼핑 토글 시 API 요청과 헤더 변경
- 로딩 상태
- 오류와 재시도
- 빠른 토글 시 유형별 응답 유지

최종 완료 조건:

```bash
npm test
npm run lint
npm run typecheck
npm run build
```

모든 명령이 성공하고, 실제 과제 페이지와 두 유형의 첫 행 및 열 표시값을 육안으로
비교해 일치해야 한다.

## 11. 다음 구현 순서와 권장 커밋

현재 프로젝트 설정을 첫 커밋으로 만든 뒤 다음 순서로 진행한다.

1. `chore: initialize React and Express project`
2. `feat: add assignment table fetcher and parser`
3. `feat: implement broadcast type tabs and table`
4. `test: cover data parsing and broadcast interactions`
5. `docs: document setup and implementation decisions`

각 단계는 해당 테스트 또는 검증 명령이 성공한 뒤 커밋한다. 제출 직전에 커밋을
하나로 squash하지 않는다.

## 12. 남은 확인 사항

- 과제를 전달받은 날짜가 기록되지 않아 `d + 7, 19:00` 마감 시각은 아직 계산하지
  않았다.
- AI 사용 규정 충돌에 대한 담당자의 서면 확인을 권장한다.
- 구현 중 원본 테이블 구조가 조사 시점과 달라졌다면 추측해서 수정하지 말고 실제
  두 유형의 HTML과 이 문서의 SSOT 우선순위를 다시 확인한다.
